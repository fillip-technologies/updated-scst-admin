<section class="bg-white border border-gray-200 rounded-lg p-4">
    <div class="flex flex-wrap gap-2">
        {{-- @foreach ($alerts as $alert)
            <button
                type="button"
                class="rounded-md border border-gray-300 px-3 py-2 text-sm text-left"
                data-alert-filter="{{ $alert['filter'] }}"
                data-alert-type="{{ $alert['type'] }}">
                {{ $alert['label'] }}
            </button>
        @endforeach --}}
    </div>
</section>
